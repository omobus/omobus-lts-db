INTRODUCTION
------------

omobus-lts-db contains the SQL scripts for creating special analitic 
storage for the Decision Support Systems. 



USAGE
-----

To build the code, symply type:

$ sudo apt-get install unifdef
$ make

After the code has been build, the pgsql and mssql SQL scripts should 
be available in the project directory.
